# ключевые параметры проекта
variables:
    purpose: 'Проект с чистой коробкой 4.3'
    # БД проекта
    database: 'rx4325_BoxOnly'
    # Домашняя директория, относительно которой хранятся все данные сервисов.
    # Используется только в конфигурационном файле.
    home_path: 'C:\rxprj\4325\d\BoxOnly'
    # каталог c репозиториями проекта
    home_path_src: 'C:\rxprj\4325\s'
    # репозитории
# репозитории
services_config:
    DevelopmentStudio:
        REPOSITORIES:
            repository: 
            -   '@folderName': 'BoxWork'
                '@solutionType': 'Work'
                '@url': ''
            -   '@folderName': 'Box'
                '@solutionType': 'Base'
                '@url': ''