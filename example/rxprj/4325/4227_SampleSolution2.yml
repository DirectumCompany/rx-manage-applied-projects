# ключевые параметры проекта
variables:
    purpose: 'Пример решения-2 с автотестами для RX 4.2'
    # БД проекта
    database: 'rx4227_SampleSolution2'
    # Домашняя директория, относительно которой хранятся все данные сервисов.
    # Используется только в конфигурационном файле.
    home_path: 'C:\rxprj\4227\d\SampleSolution2'
    # каталог c репозиториями проекта
    home_path_src: 'C:\rxprj\4227\s'
    # репозитории
# репозитории
services_config:
    DevelopmentStudio:
        REPOSITORIES:
            repository: 
            -   '@folderName': 'SampleSolution2'
                '@solutionType': 'Work'
                '@url': ''
            -   '@folderName': 'Box'
                '@solutionType': 'Base'
                '@url': ''
